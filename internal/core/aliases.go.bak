package core

import "pewbot/internal/kit"

// Re-export shared types for plugin ergonomics.
// These are aliases, not copies.

type TaskType = kit.TaskType
type TaskSpec = kit.TaskSpec
type TaskResult = kit.TaskResult
type SchedulerStats = kit.SchedulerStats

const (
	TaskOneTime  = kit.TaskOneTime
	TaskInterval = kit.TaskInterval
	TaskDaily    = kit.TaskDaily
	TaskWeekly   = kit.TaskWeekly
	TaskCron     = kit.TaskCron
)

type Target = kit.Target

type BroadcastSpec = kit.BroadcastSpec
type BroadcastJob = kit.BroadcastJob

type Priority = kit.Priority
type Notification = kit.Notification

const (
	PriorityLow      = kit.PriorityLow
	PriorityNormal   = kit.PriorityNormal
	PriorityHigh     = kit.PriorityHigh
	PriorityCritical = kit.PriorityCritical
)
